[pytest]
# 测试发现配置
testpaths = backend/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# asyncio 配置
asyncio_mode = auto

# 标记配置
markers =
    unit: 单元测试
    integration: 集成测试
    slow: 慢速测试

# 显示配置
addopts =
    -v
    --strict-markers
    --tb=short
